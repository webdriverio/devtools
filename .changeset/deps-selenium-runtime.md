---
"@wdio/selenium-devtools": patch
---

Pick up the merged `yazl` 2 to 3 update, the library the trace zip is written with. It is a runtime dependency, so the published range only changes when the package is released.

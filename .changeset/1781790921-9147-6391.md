---
"@wdio/devtools-service": patch
---

### 🐛 Fixes
- Adjust trace-artifact granularity and grouping in @wdio/devtools-service
  - Introduce `spec-level` zip and ndjson-directory
  - Correct `lastSelector` when different tests bleed through it
  - Insert `it()` testnames into trace data

name: Python Adapter

# Deliberately NOT triggered by packages/shared/**, even though the adapter's
# wire contract is generated from it. Doing so put a Python check on every PR
# that touched shared, which is most of them, and a red Python job would block
# work unrelated to this adapter. The trade-off: a shared change that breaks the
# contract is not caught here at the moment it lands. It is still caught by the
# drift check below on the next PR that touches this package, which regenerates
# _contract.py and fails on any diff. Revisit once the adapter is published and
# the coupling is worth the noise.
on:
  push:
    branches:
      - main
    paths:
      - packages/selenium-devtools-py/**
      - .github/workflows/python.yml
  pull_request:
    paths:
      - packages/selenium-devtools-py/**
      - .github/workflows/python.yml

permissions:
  contents: read

defaults:
  run:
    working-directory: packages/selenium-devtools-py

jobs:
  test:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        python-version: ['3.9', '3.12']
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2
      - uses: actions/setup-python@a26af69be951a213d495a4c3e4e4022e16d87065 # v5
        with:
          python-version: ${{ matrix.python-version }}
      # selenium is the adapter's only runtime dependency, and the tests that
      # pin its BiDi surface (tests/test_selenium_surface.py) skip without it —
      # so a bump that moved those internals used to pass CI green and degrade
      # at runtime. Installing the package is what makes those guards real.
      #
      # The matrix is load-bearing, not just breadth: network capture has two
      # subscription paths, one per selenium BiDi surface, and the python version
      # is what selects which selenium resolves. 3.9 caps at a pre-4.44 selenium
      # and exercises the connection path; 3.12 resolves the latest and exercises
      # the event-handler path. Dropping either job leaves one path untested.
      - name: Install the adapter and its runtime dependency
        run: pip install -e '.[test]'

      - name: Contract is in sync with shared
        run: |
          python scripts/gen_contract.py
          git diff --exit-code src/selenium_devtools/_contract.py
      - name: 🧪 Unit tests
        run: PYTHONPATH=src python -m unittest discover -s tests
